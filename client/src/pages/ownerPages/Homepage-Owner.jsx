import { Button } from "@mui/material";

const Homepage = () => {
  return (
    <main>
      <Button href="/owner/property/post">Post a property</Button>
    </main>
  );
};

export default Homepage;
